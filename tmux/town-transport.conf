# generated by residents/transport.lua — do not edit; edit the table there.
bind -n M-h if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-h } { if -F '#{pane_at_left}' { select-pane -t '{right}' } { select-pane -L } }
bind -n M-H if -F '#{m:*vim*,#{pane_current_command}}' { send-keys 5 C-w < } { resize-pane -L 5 }
bind -n M-w resize-pane -L 5
bind -n M-C-h swap-pane -t '{left-of}'
bind -n M-j if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-j } { if -F '#{pane_at_bottom}' { select-pane -t '{top}' } { select-pane -D } }
bind -n M-J if -F '#{m:*vim*,#{pane_current_command}}' { send-keys 5 C-w + } { resize-pane -D 5 }
bind -n M-s resize-pane -D 5
bind -n M-C-j swap-pane -t '{down-of}'
bind -n M-k if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-k } { if -F '#{pane_at_top}' { select-pane -t '{bottom}' } { select-pane -U } }
bind -n M-K if -F '#{m:*vim*,#{pane_current_command}}' { send-keys 5 C-w - } { resize-pane -U 5 }
bind -n M-n resize-pane -U 5
bind -n M-C-k swap-pane -t '{up-of}'
bind -n M-l if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-l } { if -F '#{pane_at_right}' { select-pane -t '{left}' } { select-pane -R } }
bind -n M-L if -F '#{m:*vim*,#{pane_current_command}}' { send-keys 5 C-w > } { resize-pane -R 5 }
bind -n M-e resize-pane -R 5
bind -n M-C-l swap-pane -t '{right-of}'
bind -n M-d if -F '#{m:*vim*,#{pane_current_command}}' { send-keys C-d } { copy-mode -e ; send-keys -X halfpage-down }
bind -n M-u if -F '#{m:*vim*,#{pane_current_command}}' { send-keys C-u } { copy-mode -e ; send-keys -X halfpage-up }
bind -n M-[ previous-window
bind -n M-] next-window
bind -n M-o if -F '#{m:*vim*,#{pane_current_command}}' { send-keys C-o } { run-shell "$TOWN talk future back" }
bind -n M-i if -F '#{m:*vim*,#{pane_current_command}}' { send-keys C-i } { run-shell "$TOWN talk future forward" }
bind -n 'M-%' split-window -h
bind -n 'M-"' split-window -v
