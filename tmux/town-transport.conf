# generated by residents/transport.lua — do not edit; edit the table there.
bind -n M-h if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-h } { if -F '#{pane_at_left}' { run-shell "$TOWN talk future move dir=h cross=1" } { select-pane -L } }
bind -n M-H resize-pane -L 5
bind -n M-C-h swap-pane -t '{left-of}'
bind -n M-j if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-j } { if -F '#{pane_at_bottom}' { run-shell "$TOWN talk future move dir=j cross=1" } { select-pane -D } }
bind -n M-J resize-pane -D 5
bind -n M-C-j swap-pane -t '{down-of}'
bind -n M-k if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-k } { if -F '#{pane_at_top}' { run-shell "$TOWN talk future move dir=k cross=1" } { select-pane -U } }
bind -n M-K resize-pane -U 5
bind -n M-C-k swap-pane -t '{up-of}'
bind -n M-l if -F '#{m:*vim*,#{pane_current_command}}' { send-keys M-l } { if -F '#{pane_at_right}' { run-shell "$TOWN talk future move dir=l cross=1" } { select-pane -R } }
bind -n M-L resize-pane -R 5
bind -n M-C-l swap-pane -t '{right-of}'
bind -n M-d if -F '#{m:*vim*,#{pane_current_command}}' { send-keys C-d } { if -F '#{m:*claude*,#{pane_current_command}}' { send-keys NPage } { copy-mode -e ; send-keys -X halfpage-down } }
bind -n M-u if -F '#{m:*vim*,#{pane_current_command}}' { send-keys C-u } { if -F '#{m:*claude*,#{pane_current_command}}' { send-keys PPage } { copy-mode -e ; send-keys -X halfpage-up } }
bind -n 'M-%' split-window -h
bind -n 'M-"' split-window -v
